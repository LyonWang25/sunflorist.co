---
import { site } from '../data/site';

interface Props {
  /** include the email button (secondary style) */
  email?: boolean;
  /** stack buttons full-width (mobile CTA style stays via CSS) */
  layout?: 'row' | 'grid';
}

const { email = false, layout = 'row' } = Astro.props;
---

<div class:list={['contact-buttons', `contact-buttons--${layout}`]}>
  <a class="btn" href={site.whatsappHref} data-chat-cta>WhatsApp</a>
  <a class="btn" href={site.wechatHref} data-chat-cta>WeChat</a>
  <a class="btn" href={site.instagramHref}>Instagram</a>
  {email && <a class="btn btn--secondary" href={site.emailHref}>Email</a>}
</div>

<style>
  .contact-buttons {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-3);
  }

  @media (max-width: 767px) {
    .contact-buttons--grid {
      flex-direction: column;
    }

    .contact-buttons--grid .btn {
      width: 100%;
    }
  }

  @media (min-width: 768px) {
    .contact-buttons--grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
